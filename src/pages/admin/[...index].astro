---
// src/pages/admin/[...index].astro
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>CMS Admin | Ali1_Visuals</title>
  </head>
  <body>
    <div id="sanity"></div>
  </body>
</html>

<script>
  import { defineConfig, renderStudio } from 'sanity';
  import { deskTool } from 'sanity/desk';
  import { cloudinarySchemaPlugin } from 'sanity-plugin-cloudinary';

  import photo from '../../../cms/schemaTypes/photo';

  const config = defineConfig({
    projectId: import.meta.env.PUBLIC_SANITY_PROJECT_ID,
    dataset: import.meta.env.PUBLIC_SANITY_DATASET,
    title: 'Ali1_Visuals Admin',
    basePath: '/admin', // This must match the folder name in src/pages
    plugins: [
      deskTool(),
      cloudinarySchemaPlugin(),
    ],
    schema: {
      types: [photo],
    },
  });

  renderStudio(document.getElementById('sanity'), config);
</script>

<style is:global>
  /* Isolated full-screen shell for Sanity Studio. */
  html, body {
    width: 100%;
    height: 100%;
    margin: 0;
    padding: 0;
    overflow: hidden;
    background: #101112;
  }

  #sanity {
    position: fixed;
    inset: 0;
    width: 100vw;
    height: 100dvh;
  }
</style>